import React from 'react';
import ReactDOM from 'react-dom/client';
import ricksresearch from './ricksresearch';
import './style.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ricksresearch />
  </React.StrictMode>
);
